name: Build and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  build-and-test:
    permissions:
      contents: read
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -switch /Applications/Xcode.app/Contents/Developer

      - name: Ensure TestConfig.plist exists
        run: |
          if [ ! -f "Tests/AWSTrackingSDKiOSTests/TestConfig.plist" ]; then
            if [ -n "${{ secrets.TEST_CONFIG_PLIST }}" ]; then
              echo "${{ secrets.TEST_CONFIG_PLIST }}" | base64 --decode > Tests/AWSTrackingSDKiOSTests/TestConfig.plist
            else
              echo "TEST_CONFIG_PLIST secret not configured; creating TestConfig.plist from template so the package can build. Integration tests will be skipped."
              cp Tests/AWSTrackingSDKiOSTests/TestConfigTemplate.plist Tests/AWSTrackingSDKiOSTests/TestConfig.plist
            fi
          fi

      - name: Build for iOS Device
        run: |
          xcodebuild -scheme AmazonLocationiOSTrackingSDK \
            -destination 'generic/platform=iOS' \
            -skipPackagePluginValidation \
            build

      - name: Build for macOS
        run: |
          xcodebuild -scheme AmazonLocationiOSTrackingSDK \
            -destination 'platform=macOS' \
            -skipPackagePluginValidation \
            build

      - name: Run Mock Tests
        run: |
          xcodebuild -scheme AmazonLocationiOSTrackingSDK \
            -destination 'platform=macOS' \
            -skipPackagePluginValidation \
            test \
            -only-testing:AWSTrackingSDKiOSTests/LocationTrackerMockTests

      - name: Run Integration Tests
        run: |
          if [ -n "${{ secrets.TEST_CONFIG_PLIST }}" ]; then
            xcodebuild -scheme AmazonLocationiOSTrackingSDK \
              -destination 'platform=macOS' \
              -skipPackagePluginValidation \
              -skip-testing:AWSTrackingSDKiOSTests/LocationPermissionTests \
              test
          else
            echo "TEST_CONFIG_PLIST secret not configured. Skipping integration tests."
          fi
